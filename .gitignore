# Agent Relay runtime state (contains workspace keys — never commit)
.agent-relay/
.mcp.json

# Local tooling caches
.npm-cache/
node_modules/

# Godot
.godot/
export_presets.cfg.bak

# Build artifacts
build/
exports/

# OS
.DS_Store

# Agent Relay broker state (contains workspace keys — never commit)
.agentworkforce/
